# Dependencias para los scripts de descarga
# Instalar con: pip install -r requirements_descarga.txt

# Core
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0
python-dotenv>=1.0.0

# Web scraping
beautifulsoup4>=4.12.0
lxml>=4.9.0
selenium>=4.15.0          # opcional, solo si scraping FNC requiere JS

# Imágenes / Kaggle
kaggle>=1.5.16
Pillow>=10.0.0

# APIs financieras y datos abiertos
fredapi>=0.5.1            # FRED St. Louis Fed
sodapy>=2.2.0             # Socrata (datos.gov.co, DANE)
wbdata>=1.0.0             # World Bank
yfinance>=0.2.30          # primario para TRM USDCOP, fallback FRED
openpyxl>=3.1.0           # leer XLSX (World Bank, ICO, IMF)

# Climáticos
xarray>=2023.10.0         # NetCDF
netCDF4>=1.6.4
rioxarray>=0.15.0         # geo-rasters
geopandas>=0.14.0         # opcional para shapefile

# NOAA / ENSO
# NOAA es CSV directo, no necesita librería extra

# Earth Engine (opcional, para CHIRPS y MODIS si tienen GEE)
# earthengine-api>=0.1.380

# Copernicus CDS (opcional para ERA5)
# cdsapi>=0.6.1

# Utilidades
zipfile36>=0.1.3
unidecode>=1.3.0          # normalizar nombres municipios
fuzzywuzzy[speedup]>=0.18.0  # matching municipios
